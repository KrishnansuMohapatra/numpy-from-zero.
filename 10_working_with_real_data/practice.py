# NumPy Lesson 10 Practice
# Working with Real Data

import numpy as np

# 1. Load the store dataset:
#    File: "store_records.csv" (skip header row)

# 2. Extract customer count (column index 1) and revenue (column index 2).

# 3. Calculate Average Revenue per Customer (Revenue / Customers) for each store.

# 4. Find the store ID with the highest revenue per customer using np.argmax().

# 5. Extract all store rows where customer count is 150 or more.

# 6. Compute the standard deviation of store revenue.
